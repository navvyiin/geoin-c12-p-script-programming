# Performance Evaluation

The included synthetic workflow is intended for teaching and reproducibility. Runtime depends on CPU, filesystem and Python environment. When comparing implementations, use the same scene dimensions, algorithm parameters and machine.
